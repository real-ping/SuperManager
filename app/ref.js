const main = document.getElementById('main');
const home = document.getElementById('home');
const container = document.getElementById('container');
const timer_top = document.getElementById('timer-top');
const scrollText = document.getElementById('scrollText');
const musicTimer = document.getElementById('musicTimer');
const timer_middle = document.getElementById('timer-middle');
const timer_bottom = document.getElementById('timer-bottom');
const stopwatchTab = document.getElementById('stopwatchTab');
const musicTimeline = document.getElementById('musicTimeline');
const musicPlayPause = document.getElementById('musicPlayPause');
const musicTimelineDot = document.getElementById('musicTimelineDot');
const windowTabCounter = document.getElementById('windowTabCounter');
const timer_middle_num = document.getElementById('timer-middle-num');
const musicControlLeft = document.getElementById('musicControlLeft');
const musicControlRight = document.getElementById('musicControlRight');
const timer_middle_lockIcon = document.getElementById('timer-middle-lockIcon');
const musicPlayPauseIcon_play = document.getElementById('musicPlayPauseIcon-play');
const musicPlayPauseIcon_pause = document.getElementById('musicPlayPauseIcon-pause');
const musicOptionsButtonWrapper = document.getElementById('musicOptionsButtonWrapper');